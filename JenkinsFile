pipeline {

agent any

parameters {
choiceParam('BRANCH_NAME',['Dev','Prod'], description: 'Select Branch Name')
}

stages {
  stage ('Branch') {
    when {
      expression {
         params.BRANCH_NAME = 'Dev'
         }
    steps {
           echo "Dev Branch is selected"
           }
           }
           }
        } 
}
